import { default as React } from 'react';
export interface TagProps {
    label: string;
    className?: string;
}
export declare function Tag({ label, className }: TagProps): React.ReactElement;
export default Tag;
