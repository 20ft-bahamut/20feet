import { default as React } from 'react';
import { EditorAttrs } from '../types/template';
export interface AboutPreviewProps {
    className?: string;
    editorAttrs?: EditorAttrs;
}
export declare function AboutPreview({ className, editorAttrs }: AboutPreviewProps): React.ReactElement;
export default AboutPreview;
