import { default as React } from 'react';
import { EditorAttrs } from '../types/template';
export interface HomeProcessProps {
    className?: string;
    editorAttrs?: EditorAttrs;
}
export declare function HomeProcess({ className, editorAttrs }: HomeProcessProps): React.ReactElement;
export default HomeProcess;
